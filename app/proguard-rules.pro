# HALCYON is not minified in release (isMinifyEnabled = false), so these rules
# are a placeholder for when shrinking is enabled. Compose is keep-safe via its
# own consumer rules; add app-specific keeps here if minification is turned on.
